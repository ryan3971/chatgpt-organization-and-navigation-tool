// import * as Constants from '../constants';


// function chromeStorageUpdate(key, value, activeSpace) {
//     switch (key) {
//         case Constants.NODE_SPACES_KEY:
//             setSpaces(value);
//             break;

//         case activeSpace:
//             const { nodesData, edgesData } = transformStorageData(value);
//             setNodes(nodesData);
//             setEdges(edgesData);

//         default:
//             console.log("Change to storage does not impact current render")
//             break;
//     }
// }