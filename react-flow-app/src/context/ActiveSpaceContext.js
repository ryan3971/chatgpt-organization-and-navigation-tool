// import { createContext, useContext } from "react";

// export const ActiveSpaceContext = createContext(null);

// const initialActiveSpace = null;

// export function ActiveSpaceProvider({ children }) {
// 	const [activeSpace, dispatch] = useReducer(activeSpaceReducer, initialActiveSpace);

// 	return (
// 		<TasksContext.Provider value={tasks}>
// 			<TasksDispatchContext.Provider value={dispatch}>{children}</TasksDispatchContext.Provider>
// 		</TasksContext.Provider>
// 	);
// }

// export function useActiveSpace() {
// 	return useContext(ActiveSpaceContext);
// }

